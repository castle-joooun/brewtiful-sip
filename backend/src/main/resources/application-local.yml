spring:
  datasource:
    url: jdbc:mysql://localhost:3307/brewtiful?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME:brewtiful}
    password: ${DB_PASSWORD:brewtiful}
    driver-class-name: com.mysql.cj.jdbc.Driver
